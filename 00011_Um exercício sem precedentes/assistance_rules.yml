---
rules:
- when:
    these_expectations_failed:
    - pode_se_concentrar Not:UsesIf
  then: não é necessário usar alternativa condicional em `pode_se_concentrar` já que
    podemos incluir a condição do `if` como parte do que retorne
